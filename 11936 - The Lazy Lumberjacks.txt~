Need to Learn Some English Word :
__________________________________

Lumberjack = a person who fells trees, cuts them into logs, or transports them to a sawmill ;
logs = "korat" ;
foreman = superviser ;
__________________________________

#include <bits/stdc++.h>
#define LL long long

using namespace std;

int main()
{
    int a;
    int b;
    int c;

    int test;
    cin >> test;

    while(test --)
    {
        cin >> a >> b >> c;
        if ( (a+b) > c && (b+c) > a && (c+a) > b )
        {
            printf("OK\n");
        }
        else
        {
            printf("Wrong!!\n");
        }
    }
    return 0;
}
